#!/bin/bash

# Extract raw data
name=$(jq -r '.name' data.json)
height_dm=$(jq -r '.height' data.json)
weight_hg=$(jq -r '.weight' data.json)
type=$(jq -r '.types[0].type.name' data.json)

# Use awk to convert height and weight
read formatted_height formatted_weight < <(awk -v h="$height_dm" -v w="$weight_hg" 'BEGIN { printf "%.1f %.0f", h/10, w/10 }')

# Capitalize with sed
name=$(echo "$name" | sed 's/.*/\u&/')
type=$(echo "$type" | sed 's/.*/\u&/')

# Print output in the exact requested format
echo "$name is of type $type, weighs ${formatted_weight}kg, and is ${formatted_height}m tall."
